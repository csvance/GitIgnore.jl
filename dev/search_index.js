var documenterSearchIndex = {"docs":
[{"category":"section","location":"benchmarks/#Benchmarks","page":"Benchmarks","text":"The question this package exists to answer is whether a tool should ask the git binary. bench/benchmark.jl measures that:\n\njulia --project=. bench/benchmark.jl            # subprocess cost sampled\njulia --project=. bench/benchmark.jl --full     # one git process per path\n\nIt builds a repository under the system temporary directory: 14,251 entries, of which 1,505 files survive the ignore rules, with a 10,000 file build/ directory and a 2,000 file node_modules/ both excluded, .gitignore files at three depths, and scattered *.log files with one re-included by name. The tree is built on local disk on purpose. Building it on a network filesystem measures the filesystem rather than the matcher.","title":"Benchmarks"},{"category":"section","location":"benchmarks/#Results","page":"Benchmarks","text":"Julia 1.12.6, git 2.43.0, Linux, tree on local disk, best of three runs.\n\noperation seconds vs pruned walk\nwalkfiltered, whole tree pruned 0.0014 1.0x\nwalkdir, no ignore handling at all 0.0086 6.0x\nisignored for every entry, no pruning 0.0046 3.3x\ngit check-ignore --stdin, one process 0.0162 11.3x\ngit ls-files -o -i --directory, one process 0.0027 1.9x\ngit check-ignore, one process per path 18.63 13,027x\n\nPer-path subprocess cost: 1.307 ms, of which almost all is process spawn. An earlier measurement of the same design on a 13,700 file tree put it at 2.12 ms per path and 29 s for the tree; the difference is the machine and the disk, not the shape of the result.\n\nThe in-process rows are what they are because of the two optimisations recorded in the optimisation record. Before them the pruned walk cost 0.0021 s and the per-entry queries 0.0090 s.","title":"Results"},{"category":"section","location":"benchmarks/#Reading-the-table","page":"Benchmarks","text":"Honouring .gitignore is faster than ignoring it. The pruned walk beats a plain walkdir by 6x, because it never descends into the 12,700 entries the rules exclude. Pruning at the directory level is what buys that, and it is also git's semantics: a rule inside an excluded directory cannot re-include anything.\n\nPer-path subprocesses are not a design. 18 s for one tree, and the cost is spawn, not matching, so it does not improve with a warmer cache.\n\nBatching helps and is still the wrong shape. One check-ignore --stdin process for all 14,251 paths costs 16.2 ms, 11x the pruned walk, and it cannot prune: the caller must enumerate the whole tree, including the build directory it was trying to avoid, before it can ask.\n\nThe best subprocess design is close, and still loses on everything else. One git ls-files -o -i --exclude-standard --directory costs 2.7 ms, within 2x of the pruned walk on this tree. It also needs a git binary, needs a real repository, pays a whole-repository scan per query, and fails silently: outside a repository it exits 128 with empty output, which a caller reads as \"nothing is ignored\". In process there is no binary to find, no repository required, and a directory with no rules in it costs one string comparison per entry.","title":"Reading the table"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"Four exported names. Everything else in the module is internal and may change without a version bump.","title":"API Reference"},{"category":"section","location":"api/#Compiling-the-rules","page":"API Reference","text":"","title":"Compiling the rules"},{"category":"section","location":"api/#Asking-about-a-path","page":"API Reference","text":"","title":"Asking about a path"},{"category":"section","location":"api/#Walking-a-tree","page":"API Reference","text":"","title":"Walking a tree"},{"category":"section","location":"api/#Index","page":"API Reference","text":"","title":"Index"},{"category":"module","location":"api/#GitIgnore.GitIgnore","page":"API Reference","text":"GitIgnore\n\nGit-faithful .gitignore matching and a pruning directory walk, with no dependencies outside the standard library.\n\nBuild an IgnoreMatcher for the top of a tree, ask isignored about a path, and use walkfiltered to walk the tree with ignored subtrees pruned rather than filtered out afterwards.\n\nThe verdicts are checked against the real git binary: the test suite compares this package with git check-ignore over generated trees and fails on any disagreement. libgit2 is not used, because it disagrees with git in at least two ways this package gets right; the README has the detail.\n\n\n\n\n\n","title":"GitIgnore.GitIgnore"},{"category":"type","location":"api/#GitIgnore.IgnoreMatcher","page":"API Reference","text":"IgnoreMatcher(root; excludes=true) -> IgnoreMatcher\nIgnoreMatcher(root, sources) -> IgnoreMatcher\n\nThe ignore rules in force under root, which becomes the matcher's own notion of the top of the tree.\n\nRules are read only from root and below. A .gitignore above root governs a tree the caller did not ask about and is never consulted, and neither is core.excludesFile nor any other per-user or system-wide exclude, because reading those needs git configuration this package does not parse. root need not be a repository: every .gitignore and .git/info/exclude found on the way down applies to its own subtree, the way git applies it, so a matcher rooted at a home directory still honours each repository it contains.\n\nEach directory's rules are read once, on first use, and cached in the matcher, so repeated queries over one tree pay for a .gitignore at most once. Reading is guarded by a lock, which makes one matcher safe to share across threads.\n\nPass excludes=false to skip .git/info/exclude and honour .gitignore files only.\n\nThe two-argument form takes rules the caller already holds instead of reading any: sources is a collection of prefix => content pairs, where prefix is the declaring directory relative to root (\"\" for root itself) and content is the text of an ignore file. Several pairs may share a prefix, in which case they apply in the order given. Such a matcher never touches the filesystem for rules, which makes it useful for testing pattern semantics and for a caller whose ignore rules do not live on disk. IgnoreMatcher(root, []) therefore has no rules at all, which is the way to walk a tree with only walkfiltered's .git skipping in force.\n\nExamples\n\nmatcher = IgnoreMatcher(\"/path/to/repo\")\nisignored(matcher, \"build/out.o\")\n\ninmemory = IgnoreMatcher(\".\", [\"\" => \"*.log\\n\", \"pkg\" => \"!keep.log\\n\"])\nisignored(inmemory, \"pkg/keep.log\", false)\n\n# One matcher above several checkouts. There is no separate interface for this:\n# each repository's rules govern its own subtree, so one basename gets different\n# answers in different repositories.\ncheckouts = IgnoreMatcher(\"/home/me/src\")\nisignored(checkouts, \"app/debug.log\")     # true, app/.gitignore holds *.log\nisignored(checkouts, \"lib/debug.log\")     # false, lib has no rule for logs\nisignored(checkouts, \"notes/debug.log\")   # false, notes is not a repository\n\nSee also isignored, walkfiltered.\n\n\n\n\n\n","title":"GitIgnore.IgnoreMatcher"},{"category":"function","location":"api/#GitIgnore.ignoreroot","page":"API Reference","text":"ignoreroot(matcher) -> String\n\nThe absolute path the matcher's rules and relative paths are anchored to.\n\n\n\n\n\n","title":"GitIgnore.ignoreroot"},{"category":"function","location":"api/#GitIgnore.isignored","page":"API Reference","text":"isignored(matcher, path) -> Bool\nisignored(matcher, path, is_dir::Bool) -> Bool\n\nWhether path is excluded by the rules matcher holds, matching what git check-ignore reports for the same tree.\n\npath is either absolute or relative to the matcher's root, and need not exist. A path containing .. is rejected, and so is an absolute path outside the root.\n\nDirectory-ness matters, because a pattern with a trailing slash matches directories only. The two-argument form asks the filesystem, treating a symlink as a file however it resolves and a path that cannot be stat'ed as a file, which is both git's view and walkdir's. Pass is_dir when the caller already knows, which is also the only way to ask about a path that does not exist.\n\nAn excluded directory takes everything below it: git cannot re-include a path whose parent directory is excluded, so each parent is tested first and a match there is the answer. That makes isignored cost one test per path component.\n\n.git is not special here. Git does not report it as ignored either, since it is never a candidate for tracking in the first place; walkfiltered is what skips it.\n\nExamples\n\nmatcher = IgnoreMatcher(repo)\nisignored(matcher, \"build\")            # true when `build/` is a rule\nisignored(matcher, \"build/out.o\")      # true as well, from the parent\nisignored(matcher, \"build\", false)     # false: the rule wants a directory\n\n\n\n\n\n","title":"GitIgnore.isignored"},{"category":"function","location":"api/#GitIgnore.walkfiltered","page":"API Reference","text":"walkfiltered(f, matcher, start=ignoreroot(matcher); skipgit=true)\n    -> (; completed::Bool, skipped::Int)\n\nWalk start depth first, pruning ignored entries, and call f(dir, dirs, files) once per surviving directory with the surviving entry names, dir absolute. f returns false to stop the walk, which makes completed false. start is the matcher's root by default, and is otherwise either absolute or relative to that root, which it has to be inside.\n\nPruning happens at the directory level, so an ignored directory is never descended into. That is git's semantics, where a rule inside an excluded directory cannot re-include anything, and it is the reason this walk is cheaper than walking the whole tree and filtering afterwards.\n\nskipped counts the entries the ignore rules removed, so a caller can tell an empty result caused by the rules from one caused by its own pattern. It counts entries, not files: a pruned directory holding a thousand files counts once. Entries removed by skipgit are not counted, because nothing about a repository was hidden from the caller.\n\nskipgit drops any entry named .git. It holds packed objects, so walking it is never what a caller meant, and a .git file is a worktree pointer rather than content. Git itself does not report .git as ignored, so isignored does not either; this is where it is skipped.\n\nSymlinks are never followed, and a symlinked directory is reported as a file. start itself is never pruned: a caller that names a path has asked for it, the same way rg dist/ searches dist/.\n\nA directory that cannot be read is skipped rather than throwing, which keeps one unreadable directory from failing a whole walk.\n\nExamples\n\nmatcher = IgnoreMatcher(repo)\nsources = String[]\nwalkfiltered(matcher, repo) do dir, dirs, files\n    append!(sources, joinpath(dir, f) for f in files if endswith(f, \".jl\"))\n    return true\nend\n\n\n\n\n\n","title":"GitIgnore.walkfiltered"},{"category":"section","location":"guide/#Guide","page":"Guide","text":"","title":"Guide"},{"category":"section","location":"guide/#Installation","page":"Guide","text":"julia> using Pkg; Pkg.add(\"GitIgnore\")\n\nThe package has no dependencies outside the standard library.","title":"Installation"},{"category":"section","location":"guide/#Building-a-matcher","page":"Guide","text":"IgnoreMatcher compiles the rules in force under a directory, which becomes the matcher's own notion of the top of the tree:\n\nusing GitIgnore\n\nmatcher = IgnoreMatcher(\"/path/to/repo\")\n\nThe root need not be a repository. Every .gitignore and .git/info/exclude found on the way down applies to its own subtree, the way git applies it, so a matcher rooted at a home directory still honours each repository it contains. Nothing above the root is ever consulted: a .gitignore up there governs a tree the caller did not ask about.\n\nEach directory's rules are read once, on first use, and cached in the matcher, so repeated queries over one tree pay for a .gitignore at most once. Reading is guarded by a lock, so one matcher can be shared across threads. A matcher is a snapshot of what it has read, so build a new one to pick up an edited .gitignore.\n\nPass excludes = false to honour .gitignore files only and skip .git/info/exclude.","title":"Building a matcher"},{"category":"section","location":"guide/#Asking-about-one-path","page":"Guide","text":"isignored takes a path that is either absolute or relative to the matcher's root, and need not exist:\n\nisignored(matcher, \"build\")            # true when `build/` is a rule\nisignored(matcher, \"build/out.o\")      # true as well, taken from the parent\nisignored(matcher, \"build\", false)     # false: the rule wants a directory\nisignored(matcher, \"src/main.jl\")      # false\n\nTwo things are worth knowing about the answer.\n\nDirectory-ness matters, because a pattern with a trailing slash matches directories only. The two-argument form asks the filesystem, treating a symlink as a file however it resolves. Pass the third argument when the caller already knows, which is also the only way to ask about a path that does not exist.\n\nAn excluded directory takes everything below it. Git cannot re-include a path whose parent directory is excluded, so each parent is tested first and a match there is the answer, which makes a query cost one test per path component.","title":"Asking about one path"},{"category":"section","location":"guide/#Walking-a-tree","page":"Guide","text":"walkfiltered walks depth first and prunes as it goes, calling the callback once per surviving directory:\n\nsources = String[]\nresult = walkfiltered(matcher, \"/path/to/repo\") do dir, dirs, files\n    append!(sources, joinpath(dir, name) for name in files if endswith(name, \".jl\"))\n    return true                        # `false` stops the walk\nend\n\nresult.completed                       # false when the callback stopped it\nresult.skipped                         # entries the rules removed\n\nPruning happens at the directory level, so an ignored directory is never descended into. That is git's semantics, and it is why this is cheaper than walking the whole tree and filtering afterwards.\n\nskipped counts entries rather than files, so a pruned directory holding a thousand files counts once. It exists so a caller can tell an empty result caused by the ignore rules from one caused by its own pattern.\n\n.git is dropped by name, which skipgit = false turns off. Git itself does not report .git as ignored, so isignored does not either; the walk is where it is skipped.\n\nSymlinks are never followed, and a symlinked directory is reported as a file, which is both git's view and walkdir's default. A directory that cannot be read is skipped rather than throwing, so one unreadable directory does not fail a whole walk.","title":"Walking a tree"},{"category":"section","location":"guide/#Nested-repositories","page":"Guide","text":"The root does not have to be a repository, and there is no separate interface for the case where it is not: one matcher covers whatever it finds below it. Every .gitignore and .git/info/exclude on the way down applies to its own subtree, which is what git does, so a tool pointed at a directory full of checkouts gets each repository's rules without being told where they are.\n\nGiven this tree, where app and lib are separate repositories and notes is not a repository at all:\n\nsrc/                        <- the matcher root, not a repository\n├── app/\n│   ├── .git/\n│   ├── .gitignore          build/  *.log  !keep.log\n│   ├── build/out.o\n│   ├── debug.log\n│   ├── keep.log\n│   └── main.jl\n├── lib/\n│   ├── .git/info/exclude   scratch.txt\n│   ├── .gitignore          *.o\n│   ├── debug.log\n│   ├── scratch.txt\n│   ├── util.jl\n│   └── util.o\n└── notes/\n    ├── debug.log\n    └── todo.md\n\none matcher answers for all of it:\n\nmatcher = IgnoreMatcher(\"src\")\n\nisignored(matcher, \"app/debug.log\")      # true, app's *.log\nisignored(matcher, \"app/keep.log\")       # false, app re-included it by name\nisignored(matcher, \"app/build\")          # true, app's build/\nisignored(matcher, \"app/build/out.o\")    # true, taken from the excluded parent\nisignored(matcher, \"lib/util.o\")         # true, lib's *.o\nisignored(matcher, \"lib/scratch.txt\")    # true, lib's .git/info/exclude\nisignored(matcher, \"lib/debug.log\")      # false, lib has no rule for logs\nisignored(matcher, \"notes/debug.log\")    # false, no repository, no rules\n\nThe three debug.log files are the point: one basename, three answers, because each repository's rules stop where the repository does.\n\nThe walk behaves the same way, pruning each repository by its own rules and skipping every .git it meets, at any depth:\n\njulia> walkfiltered(matcher, \"src\") do dir, dirs, files\n           println(rpad(relpath(dir, \"src\"), 6), \" dirs=\", dirs, \" files=\", files)\n           return true\n       end\n.      dirs=[\"app\", \"lib\", \"notes\"] files=String[]\napp    dirs=String[] files=[\".gitignore\", \"keep.log\", \"main.jl\"]\nlib    dirs=String[] files=[\".gitignore\", \"debug.log\", \"util.jl\"]\nnotes  dirs=String[] files=[\"debug.log\", \"todo.md\"]\n(completed = true, skipped = 4)\n\nskipped is 4: app/build, app/debug.log, lib/util.o and lib/scratch.txt. The .git directories are not counted, because skipping them hides nothing about a repository.\n\nRooting a matcher at one of the repositories instead gives that repository's semantics and nothing else, and the root is a boundary the query cannot cross:\n\napp = IgnoreMatcher(\"src/app\")\n\nisignored(app, \"debug.log\")              # true\nisignored(app, \"../lib/util.o\")          # ArgumentError: not inside the root\n\nA nested repository's .git/info/exclude being honoured is deliberate, and it is the one place this diverges from git run from above that repository, which reads only the outermost exclude file. Honouring a nested repository's .gitignore but not its excludes would honour half its rules. excludes = false turns exclude files off everywhere and leaves .gitignore alone:\n\nmatcher = IgnoreMatcher(\"src\"; excludes = false)\n\nisignored(matcher, \"lib/scratch.txt\")    # false, the exclude file is not read\nisignored(matcher, \"lib/util.o\")         # true, .gitignore still applies","title":"Nested repositories"},{"category":"section","location":"guide/#Rules-that-are-not-on-disk","page":"Guide","text":"The two-argument constructor takes prefix => content pairs, where prefix is the declaring directory relative to the root and content is the text of an ignore file. Such a matcher never touches the filesystem for rules:\n\ninline = IgnoreMatcher(\".\", [\"\" => \"*.log\\n\", \"pkg\" => \"!keep.log\\n\"])\nisignored(inline, \"a.log\", false)          # true\nisignored(inline, \"pkg/keep.log\", false)   # false, the nested line wins\n\nSeveral pairs may share a prefix, in which case they apply in the order given, which is how a directory's excludes and its .gitignore combine.\n\nIgnoreMatcher(root, []) therefore has no rules at all, which is the way to walk a tree with only the walk's .git skipping in force:\n\nwalkfiltered(IgnoreMatcher(root, []), root) do dir, dirs, files\n    return true                        # everything except .git\nend","title":"Rules that are not on disk"},{"category":"section","location":"guide/#Patterns-that-are-understood","page":"Guide","text":"Everything in gitignore(5), checked against the git binary: nested precedence, ! re-inclusion scoped to a subtree, the trailing slash for directories only, * and ? stopping at a separator, ** crossing it, anchoring by a leading or interior /, bracket expressions including POSIX classes, git's rule that only unescaped trailing spaces are stripped, CRLF, a leading byte order mark, and the inert-on-malformed-line behaviour that keeps one unusable line from costing the whole file.\n\nThe exceptions are listed under Fidelity to git.","title":"Patterns that are understood"},{"category":"section","location":"fidelity/#Fidelity-to-git","page":"Fidelity to git","text":"The package's one distinguishing claim is that its verdicts match git's. This page is what that claim rests on, and where it stops.","title":"Fidelity to git"},{"category":"section","location":"fidelity/#How-it-is-checked","page":"Fidelity to git","text":"test/differential.jl builds fixture trees in temporary repositories, asks this package and git check-ignore --no-index --stdin -z about every path in each one, and fails on any disagreement. Without -v that command prints exactly the ignored paths, so its output is the answer rather than something to interpret, and every call runs with the user's git configuration neutralised, since a global core.excludesFile would otherwise make git ignore files this package never reads.\n\nBoth answers are compared, because they are different code reaching the same conclusion: the per-path verdict from isignored, and the surviving set the pruning walk reports.\n\nFourteen hand-built fixtures cover nested negation, negation below an excluded directory, symlinks, anchoring and the ** forms, the ignore-everything-then-re-include idiom, repository-local excludes, whitespace and CRLF and BOM handling, bracket expressions, POSIX classes, ranges a regex engine would reject, non-ASCII names, a nested repository, and rules at five depths. Around 780 further sweeps come from a table of 60 awkward patterns and a deterministically generated set of pattern shapes, each rewriting one ignore file and re-checking every path in a 30 entry tree, run through the root .gitignore, a nested .gitignore, and .git/info/exclude: roughly 24,000 path verdicts in all.\n\nThe suite skips itself with a message when no git binary is present, so the package still tests on a machine without git. A run that skipped it proves nothing about fidelity.\n\nGenerated fixtures are not the same thing as real ignore files, so bench/realworld.jl runs the same comparison over any checkouts you point it at:\n\njulia --project=. bench/realworld.jl ~/src/one ~/src/two\n\nOver 24 checkouts on the author's machine, 371,576 paths, it found no disagreement.","title":"How it is checked"},{"category":"section","location":"fidelity/#Why-not-libgit2","page":"Fidelity to git","text":"Julia ships LibGit2, which exposes git_ignore_path_is_ignored. It is the obvious thing to reach for, and it disagrees with git. Both of the following were reproduced against libgit2 1.9 with a raw ccall on a fresh repository handle and no traversal, so they are libgit2's behaviour rather than an artifact of how it was called, and both are named regression tests here.\n\nA negation in a nested .gitignore does not override a shallower pattern. With *.tmp at the root and !important.tmp in pkg/.gitignore, git 2.43 does not ignore pkg/important.tmp. libgit2 ignores it.\n\nA dir_only pattern matches a symlink to a directory. With link/ in .gitignore, where link is a symlink pointing at a directory, git does not ignore link, because a symlink is a file however it resolves. libgit2 ignores it.\n\nBoth are the kind of divergence that hides a file the caller needed, silently.","title":"Why not libgit2"},{"category":"section","location":"fidelity/#Why-not-shell-out-to-git","page":"Fidelity to git","text":"Because it does not scale, and because it fails quietly. On a 14,251 entry tree on local disk, one git check-ignore process per path costs 18.6 s against 4.6 ms for the same queries in process, and the pruning walk answers the same question in 1.4 ms. Batching every path through one check-ignore --stdin process is affordable but cannot prune, so it pays for the whole tree whatever the caller wanted. The full table and its method are in Benchmarks.\n\nThe correctness argument matters more than the speed: git check-ignore needs a git binary and a real repository, and outside one it exits 128 with empty output, which reads exactly like \"nothing here is ignored\".","title":"Why not shell out to git"},{"category":"section","location":"fidelity/#What-is-deliberately-not-supported","page":"Fidelity to git","text":"Per-user and system-wide excludes are not read. core.excludesFile, $XDG_CONFIG_HOME/git/ignore and the system gitignore need git configuration this package does not parse. Only .gitignore files and .git/info/exclude are consulted, and excludes = false narrows that to .gitignore alone.\nNothing above the root is consulted. A .gitignore above the directory the matcher was built for governs a tree the caller did not ask about.\nThe index is not consulted. These are pattern verdicts, matching git check-ignore, not git status. A tracked file whose name matches a pattern is reported as ignored, and .git is reported as ignored when a pattern actually matches it, which is what check-ignore does too.\nCase is significant. core.ignorecase, which git sets on case-insensitive filesystems, is not modelled. On Linux this is git's own behaviour; on macOS and Windows it is a real divergence.\n[=a=] and [.a.] are inert. Equivalence classes and collating elements are git syntax that PCRE does not implement, so a pattern containing one matches nothing rather than being mistranslated into a pattern that hides the wrong files. [[:digit:]] and the rest of the POSIX classes work.\nSymlinks are never followed by the walk, and a symlinked directory is reported as a file.\nNested repository excludes are honoured, which is a deliberate divergence: git run above a nested repository reads only the outermost .git/info/exclude, while a matcher rooted above several repositories reads each one's. Honouring a nested repository's .gitignore but not its excludes would honour half its rules.","title":"What is deliberately not supported"},{"category":"section","location":"fidelity/#The-limits-of-the-guarantee","page":"Fidelity to git","text":"The suite proves agreement with the git version it ran against, on the platform it ran on, for the cases it covers. It does not prove anything about the exclusions above, and the case-sensitivity gap is the one that would bite a macOS or Windows user first.\n\nTwo translation limits are worth naming. [=a=] and [.a.] are inert, as above. And a bracket range a regex engine rejects outright is emulated rather than translated: git's wildmatch compares each member literally as it reads it and applies a range only when it reaches the -, so [c-a] matches c and nothing else. That behaviour is reproduced deliberately, and swept against git.","title":"The limits of the guarantee"},{"category":"section","location":"optimisation/#Optimisation-record","page":"Optimisation record","text":"What was considered, what was measured, and what was rejected. The standing rule is that a change trading git fidelity for speed or brevity is not worth it, so most of this document is recorded rejections.\n\nEvidence rules: a claim of a performance win needs a measurement on a tree built on local disk, since ~/Git is NFS mounted and timings there are an order of magnitude slower and dominated by the filesystem. Anything unmeasured is labelled speculation and is not acted on. A review pass by a separate agent produced the measurements below on two trees: an 11,506 entry tree with 1,416 directories and 32 ignore files at four depths, and the 14,251 entry tree bench/benchmark.jl builds. Both were re-measured here before anything was applied.\n\nEvery applied change was checked the same way: the differential suite green (220 tests at the time, 224 now, including 34 comparisons against git 2.43), and bench/realworld.jl over 24 real checkouts, 371,576 paths, with no disagreement in either the per-path verdict or the pruning walk.","title":"Optimisation record"},{"category":"section","location":"optimisation/#Applied","page":"Optimisation record","text":"","title":"Applied"},{"category":"section","location":"optimisation/#The-walk-no-longer-stats-for-ignore-files-it-can-already-see","page":"Optimisation record","text":"walkfiltered used to load a child directory's rules when it queued the child, which is before it has listed it, so load_dir_rules had no choice but to stat for .gitignore and for .git/info/exclude in every directory in the tree. On the 11,506 entry tree that is 2,832 stat calls, at 3.6 us each on tmpfs and about 16 us each on NFS, to find 32 ignore files.\n\nThe load now happens when the directory is reached rather than when it is queued, and dir_entries returns two flags with the listing it was building anyway: whether .git and .gitignore are among the names. Neither file can exist unless its name is in that listing, so two stat calls become two string comparisons in a loop that already ran. isignored still probes, since it has no listing.\n\nMeasured, isolating rule loading as a fresh-matcher walk minus a warm-matcher walk: 10.15 ms to 1.30 ms on the 11,506 entry tree, 0.42 ms to 0.15 ms on the benchmark tree, 6.7 ms to 3.5 ms on an NFS tree. It scales with directory count, so it is invisible on a shallow tree and dominant on a deep one, and it grows in absolute terms on a network filesystem.\n\nThe one behavioural difference is that a directory that cannot be listed no longer gets a cache entry, which no verdict depends on. The set of files read is unchanged, and the guard in load_ignore_patterns still covers a .gitignore that is a directory and a .git that is a worktree pointer file.","title":"The walk no longer stats for ignore files it can already see"},{"category":"section","location":"optimisation/#Unanchored-patterns-are-basename-tests,-and-are-matched-as-such","page":"Optimisation record","text":"A pattern body with no / in it compiled to ^(?:.*/)?BODY$ and was matched against the whole relative path. But no such body can match a /: the translation only emits [^/]*, [^/], (?!/)[...] and escaped literals, and a body containing a literal or escaped / is anchored by definition. So the regex matches a path exactly when ^BODY$ matches that path's last segment, and the cheap shapes of that question need no regex at all. Patterns are now classified at parse time into four kinds: the name equals a literal, the name ends with a literal, a regex against the name, or a regex against the path. The one unanchored body that can cross a separator is one made of nothing but stars, which compiles to .*, so it stays a path test.\n\n before after change\nwarm pruned walk, 11,506 entries 14.09 ms 9.31 ms -34%\nisignored over all 11,506 entries 36.46 ms 11.36 ms -69%\nisignored over all 14,251 entries 8.50 ms 4.17 ms -51%\nmatch cost alone, rules that never match 7.30 ms 1.21 ms -83%\n\nPer operation: occursin with the unanchored regex against a six segment path is 139 ns, the anchored regex against the name is 42 ns, endswith is 4.6 ns, and == is 4.3 ns. The end-to-end effect on bench/benchmark.jl is the pruned walk going from 2.1 ms to 1.4 ms and the per-entry queries from 9.0 ms to 4.6 ms.\n\nThe correctness of this rests on the equivalence argument above, which is exactly the kind of claim the differential suite exists to check, and it is checked: the non-ASCII, whitespace, bracket and POSIX class fixtures all pass, as do the 780 swept patterns and the 371,576 real paths.\n\nTwo decisions inside this change are worth recording. The regex is still compiled for the literal and suffix kinds even though nothing matches against it, which costs 0.24 to 0.54 ms on a fresh walk: it keeps the faithful translation as the single source of truth, and it keeps the option of cross-checking the fast path against it. And prefix stripping for a nested rule set now returns a SubString view rather than copying the path, which the old code did per rule set per entry.","title":"Unanchored patterns are basename tests, and are matched as such"},{"category":"section","location":"optimisation/#What-was-left-out-of-that-change","page":"Optimisation record","text":"The review also proposed computing each rule set's view of the current directory once per directory in the walk, rather than stripping per entry, and adding a needs_path flag to IgnoreRules so the relative path is not built at all when no rule needs it. Measured at about 2% of the walk, in exchange for a new field and a second copy of the match loop. Not taken: the lazy form above gets the same result for the common case, since a root rule set has an empty prefix and stripping it is already free.","title":"What was left out of that change"},{"category":"section","location":"optimisation/#Allocations","page":"Optimisation record","text":"A separate pass with BenchmarkTools and Profile.Allocs at sample_rate = 1, which attributes every allocation to the line that made it, over a 12,048 entry tree on local disk. Wall-clock time did not move outside noise in any of this; what follows is about allocations.\n\n before after\nwalkfiltered, whole tree, warm matcher 4,579 allocs, 0.33 MiB 3,261 allocs, 0.25 MiB\nisignored, absolute path 58 allocs, 3,296 B, 4.59 us 13 allocs, 560 B, 0.64 us\nisignored, three segment relative path 9 allocs, 336 B 9 allocs, 336 B\ndir_entries, 31 entry directory 42 allocs, 3,248 B 40 allocs, 2,960 B\n\nAn absolute path no longer goes through relpath. Nearly all of the 58 allocations were one line: relpath(abspath(path), root), which splits both paths into components and compares them. A path under the root needs the root sliced off the front instead, which is a startswith and a view. relpath is kept for the case the slice cannot answer, a path that is not under the root at all, where the .. it produces is what turns the caller's mistake into the ArgumentError they should get. Asking about an absolute path is the natural thing to do with a path that came from walkdir, and it was the worst number in the profile by an order of magnitude.\n\nThe walk no longer builds a path per entry when nothing reads one. A path is read only by an anchored pattern or to decide whether a nested rule set applies, and most ignore files contain neither, so IgnoreRules now records whether any of its patterns looks at the path, and the walk checks once per directory whether any rule in scope wants one. Where none does, the entry's own name stands in. That was 1,843 of the walk's allocations on this tree, and 1,210 of them went away; the rest are the two directories here that do have a nested rule set.\n\ndir_entries sizes its vector from the listing rather than growing it an entry at a time, since both listing functions return something with a length.\n\nThe segment split moved into its own method. Not a saving on its own: it is what makes the first item safe, because a variable that is sometimes a String and sometimes a view costs an allocation at every use of it.","title":"Allocations"},{"category":"section","location":"optimisation/#Tried,-measured,-reverted","page":"Optimisation record","text":"Recorded because the measurement is the only reason to prefer the code that is there, and because three of the four were the same Julia mistake.\n\nSegments as views instead of copies. Vector{SubString{String}} avoids copying each segment, and cost more: the same allocation count, more bytes, because a view is three words where a string reference is one, and it made the accumulated relative path a union of two types, so path_ignored boxed its argument at every call. isignored went from 9 allocations to 12.\nsizehint! on the segment vector, from count('/', text) + 1. The count scan and the up-front buffer cost more than the growth they avoided: root_segments went from 5 allocations to 8.\nAn occursin guard before replace in normalize_relpath, on the theory that replace allocates a new string even when it changes nothing. It does not: the line never appeared in the allocation profile, before or after. No benefit, so the guard went.\nLooking the rule cache up before inserting, to keep a view-typed key from being converted on a hit. Only useful with the views above, which are gone.\n\nStill there and not worth touching: copy of the root's rules in isignored is two allocations that a growable stack has to pay; the dirs and files vectors grow in 138 allocations across the tree; and isignored could avoid materialising segments at all by taking views of one normalised path, which is the shape the walk already has, but it would need a second normalisation path beside split_segments and that is where a fidelity bug would hide.","title":"Tried, measured, reverted"},{"category":"section","location":"optimisation/#Rejected","page":"Optimisation record","text":"","title":"Rejected"},{"category":"section","location":"optimisation/#Flattening,-sharing-or-caching-the-per-directory-rule-stack","page":"Optimisation record","text":"The premise did not survive contact with the code. walkfiltered only vcats when a child directory actually contributes rules, so on the 11,506 entry tree it runs 31 times across 1,363 descended directories, not once per descent, and all 31 together cost 2.3 us, or 0.02% of the walk. The copy in the stack builders is 35 ns, so 1,363 of them are 48 us. A prefix-keyed stack cache would add its own invalidation and locking story for nothing measurable. The current shape, one vector appended on the way down and shared by reference when the child adds nothing, is already right.","title":"Flattening, sharing or caching the per-directory rule stack"},{"category":"section","location":"optimisation/#A-global-regex-compilation-cache","page":"Optimisation record","text":"Regex construction is 3.5 us and a full translation is 8.7 us, so a seven pattern file is 65 us and 32 ignore files are about 1.4 ms of a fresh walk. A cache keyed by pattern body would help only a tree with many byte-identical ignore files, at most about 1 ms there and nothing on a normal repository, in exchange for process-global mutable state that has to be locked, never shrinks, and holds data derived from untrusted input. Compilation is also once per matcher rather than once per query, which is the wrong half of the workload. The applied change above is the version of this idea that pays, because it removes matching cost rather than construction cost.","title":"A global regex compilation cache"},{"category":"section","location":"optimisation/#Removing-the-matcher's-lock","page":"Optimisation record","text":"The ReentrantLock costs 25 ns per rule lookup, which is 35 us per walk of the 11,506 entry tree, or 0.35%, and about 9% of a query-heavy isignored sweep. It buys the documented and tested guarantee that one matcher can be shared across threads, which an unlocked Dict read racing a write does not provide at any Julia version. The lock-free alternative, an atomically swapped immutable snapshot, makes reads free but turns cache population into a copy per directory, which is quadratic in the number of cached prefixes and strictly worse for the one-shot walk that is the main use.","title":"Removing the matcher's lock"},{"category":"section","location":"optimisation/#Removing-the-POSIX-class-and-collating-element-machinery","page":"Optimisation record","text":"copy_posix_class and is_collating_element are reachable only from a pattern body containing [, so on a .gitignore of *.log and build/ they never execute at all, and they run at parse time in any case. The observation that [[:digit:]] and [=a=] essentially never appear in a real .gitignore is correct and is not the point: keeping them costs 30 lines that never run, and dropping them costs correctness in exactly the way this package exists to avoid. Without the first, [[:digit:]].dat becomes a class of the six characters in :digit followed by a literal ], which silently matches the wrong files. Without the second, [=a=] stops being inert and starts mistranslating. Both hide a file the caller needed.","title":"Removing the POSIX class and collating element machinery"},{"category":"section","location":"optimisation/#Removing-the-_readdirx-fallback","page":"Optimisation record","text":"HAS_READDIRX is a compile-time constant, so the branch is folded and the fallback costs nothing at runtime. It is three lines of insurance against a non-public Base function disappearing, and since dir_entries took the choice as a parameter it is no longer untested: the suite exercises both listing paths and asserts they agree. It also turned out to be the only thing standing between this package and running on Julia 1.10, where _readdirx does not exist.","title":"Removing the _readdirx fallback"},{"category":"section","location":"optimisation/#Shrinking-the-public-API","page":"Optimisation record","text":"Nothing in IgnoreMatcher, isignored, walkfiltered, ignoreroot, the excludes keyword or the explicit-sources constructor is worth removing. excludes=false is the only way to express \"honour .gitignore only\" and now costs nothing when no .git is present. The explicit-sources constructor is how pattern semantics are tested without a filesystem, is the documented spelling of \"no rules at all\", and serves a caller whose rules are not on disk, for one Bool field and one branch. ignoreroot is three lines and the only way to recover the anchor. walkfiltered's skipped cannot be reconstructed after the fact, since pruning means the caller never sees what was dropped.","title":"Shrinking the public API"},{"category":"section","location":"optimisation/#Two-micro-items","page":"Optimisation record","text":"Reusing the dirs and files vectors across directories is about 1% of the walk and would mean promising that the callback does not retain them, which the docstring does not promise and a caller reasonably violates. Making root_segments allocation-free is the largest remaining item inside isignored at 310 ns of about 970 ns per query, but isignored needs the cumulative prefix strings anyway to key the rule cache, so most of that is not recoverable; it was not prototyped and stays speculation.","title":"Two micro-items"},{"category":"section","location":"optimisation/#One-automaton-per-ignore-file,-instead-of-one-regex-per-line","page":"Optimisation record","text":"Rejected for now, and the reasoning is worth keeping because it is the most substantial version of the idea the applied change above only approximates.\n\nThe matcher compiles each .gitignore line separately and, for each path, tests every line of every applicable file in order, keeping the last match's verdict. Cost is O(lines) tests per path per ignore file in scope, whatever each test costs. The question raised was whether the lines of one ignore file could instead be compiled into a single automaton, built once when the file is read and reused for every path tested against it.\n\nThis is expressible, and the scoping makes it clean. Everything the translator emits is regular: no backreferences, no capture-dependent behaviour, and the one lookahead it emits, (?!/) in front of a bracket expression, is just \"this character is not a separator\", which folds into the class. The semantics needed are not \"does any line match\" but \"which is the last matching line\", which is the standard lexer-generator construction: label each accepting state with the highest-priority line that accepts there. dir_only needs the label to be a pair, the best line for a file query and the best line for a directory query. Compiling per ignore file rather than per rule stack also avoids the obvious objection to a whole-stack automaton, which is that a deeper .gitignore discovered during the walk would invalidate it.\n\nThe cost it removes is linear in the number of lines, and the applied basename change already took most of it. Timing isignored over 2,000 three-segment paths against an ignore file of N non-matching patterns, per path, after the optimisations above:\n\npattern shape 6 50 200 500\n*.extN, a suffix test 0.64 us 1.31 us 4.26 us 9.90 us\nfileN.txt, a literal test 0.43 us 1.13 us 3.27 us 7.77 us\n*x*N.log, needs a regex 1.33 us 10.60 us 42.92 us 133.21 us\na/b/N.log, anchored 1.39 us 10.11 us 40.47 us 123.02 us\n\nBefore the basename change every row looked like the third one: 200 patterns cost 53 us per path whatever their shape. The linear factor is still there, but it now costs 20 ns per pattern for the shapes real ignore files are mostly made of and 210 ns per pattern for the ones that need a regex. Across the 24 checkouts the real-world check runs over, the largest .gitignore is 51 lines, the median is under 20, and almost every line is a literal or a suffix, which puts a realistic repository at about 1.3 us per entry tested. An automaton would still remove the linear factor outright, and it would still be worth something on an ignore file of a few hundred regex-shaped patterns, but the absolute numbers it competes against are now an order of magnitude smaller than they were when this was first written.\n\nThere is no off-the-shelf Julia package for it. In the General registry, Automa.jl is the only serious automata compiler, and it is a regex-to-Julia code generator: its documented entry points end in |> eval, which is right for patterns known when the code is written and wrong for patterns read out of a .gitignore at runtime. Using it would mean an eval per ignore file, with the world-age, latency and precompilation problems that brings, and it would cost the zero-dependency claim, since it carries PrecompileTools and TranscodingStreams. RegularExpressions.jl is a stale 0.1.0 helper for assembling regex strings, not an engine, and there is no RE2, Hyperscan or regex-automata equivalent. So this means writing the engine here, and the hidden cost is Unicode: [[:alpha:]] and café* make the alphabet codepoints, so transitions have to be interval based rather than a 256 entry byte table, or the patterns have to be expanded into UTF-8 byte sequences.\n\nThe cheaper version of the same idea was taken instead. The shape buckets described above are what globset does for ripgrep, and they are now applied: a literal name is a ==, a *.ext pattern is an endswith, and both resolve by line index exactly as an automaton's accepting-state label would. Two further steps short of an automaton remain available and are not taken, because nothing measured justifies them yet: a capture-free union regex used purely as a reject prefilter, which would skip the loop entirely for the majority of entries that match nothing, and a priority-ordered alternation, which would answer in one regex execution but needs capture bookkeeping to identify the branch.\n\nWhatever is built, the sweep harness is what makes it safe: a fast path can be compared against both git and the naive per-pattern implementation over generated patterns, so a divergence shows up as a test failure rather than as a hidden file. That is how the basename reduction was accepted.\n\nVerdict: deferred, and now further away than it was. The shape buckets were applied and are the cheap two thirds of this idea: a literal is a == and a suffix is an endswith, and those two shapes cover most of what a real .gitignore contains. What an automaton would add on top is removing the linear factor for the regex-shaped remainder, and doing that means writing an interval-based engine by hand, for a package whose one selling point is verified fidelity. Revisit if a workload appears with hundreds of regex-shaped patterns in one ignore file; until then the measurement does not justify the surface.","title":"One automaton per ignore file, instead of one regex per line"},{"category":"section","location":"","page":"Home","text":"<div id=\"landing\" class=\"landing\">\n<header class=\"landing-hero\">\n  <div class=\"landing-hero__text\">\n    <p class=\"landing-name\">GitIgnore.jl</p>\n    <h1 class=\"landing-title\">Git-faithful .gitignore matching</h1>\n    <p class=\"landing-tagline\">A dependency-free matcher and a pruning directory walk, with every verdict checked against the real git binary rather than against the manual page.</p>\n    <div class=\"landing-actions\">\n<a class=\"landing-btn landing-btn--brand\" href=\"guide/\">Get started</a>\n      <a class=\"landing-btn landing-btn--alt\" href=\"fidelity/\">Fidelity to git</a>\n      <a class=\"landing-btn landing-btn--alt\" href=\"https://github.com/csvance/GitIgnore.jl\">View on GitHub</a>\n    </div>\n  </div>\n</header>\n<section class=\"landing-features\" aria-label=\"Features\">\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">🔍</div>\n<h2 class=\"landing-feature__title\">Checked against git</h2>\n<p class=\"landing-feature__details\">The test suite asks this package and git check-ignore about every path in generated fixture trees and fails on any disagreement, per path and through the walk.</p>\n  </div>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">✂️</div>\n<h2 class=\"landing-feature__title\">Pruning walk</h2>\n<p class=\"landing-feature__details\">An ignored directory is never descended into, which is git's own semantics and makes honouring .gitignore six times faster than a walk that ignores it.</p>\n  </div>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">🪶</div>\n<h2 class=\"landing-feature__title\">Zero dependencies</h2>\n<p class=\"landing-feature__details\">The standard library and nothing else, so it costs a registered package nothing to depend on.</p>\n  </div>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">🧭</div>\n<h2 class=\"landing-feature__title\">Nested rules</h2>\n<p class=\"landing-feature__details\">Every .gitignore and .git/info/exclude below the root governs its own subtree, so a matcher rooted at a home directory still honours each repository in it.</p>\n  </div>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">🚫</div>\n<h2 class=\"landing-feature__title\">Not libgit2</h2>\n<p class=\"landing-feature__details\">libgit2 disagrees with git on nested negation and on a symlinked directory. Both are named regression tests here.</p>\n  </div>\n  <div class=\"landing-feature\">\n<div class=\"landing-feature__icon\">⚡</div>\n<h2 class=\"landing-feature__title\">Not a subprocess</h2>\n<p class=\"landing-feature__details\">One git process per path costs 18.6 s on a 14,251 entry tree, where the pruning walk answers in 1.4 ms.</p>\n  </div>\n</section>\n</div>","title":"Home"},{"category":"section","location":"#What-it-is","page":"Home","text":"An agent or a tool pointed at a repository should see the repository, not its build output. rg solved that by honouring .gitignore and skipping .git. This package is that behaviour on its own: compile the ignore rules under a directory, ask whether a path is excluded, and walk a tree with the ignored subtrees pruned rather than filtered out afterwards.\n\nusing GitIgnore\n\nmatcher = IgnoreMatcher(\"/path/to/repo\")\nisignored(matcher, \"build/out.o\")        # true, taken from the parent directory\n\nwalkfiltered(matcher, \"/path/to/repo\") do dir, dirs, files\n    @info \"surviving\" dir files\n    return true\nend\n\nThe root need not be a repository. One matcher covers a whole directory of checkouts, and each repository's rules govern its own subtree, which is what git does:\n\ncheckouts = IgnoreMatcher(\"/home/me/src\")\n\nisignored(checkouts, \"app/build/out.o\")   # true, from app/.gitignore\nisignored(checkouts, \"lib/scratch.txt\")   # true, from lib/.git/info/exclude\nisignored(checkouts, \"notes/debug.log\")   # false, notes is not a repository\n\nThere is no separate interface for that case; see Nested repositories for the whole worked example.\n\nStart with the Guide for the whole API, or with Fidelity to git for how the verdicts are verified, what is deliberately not supported, and why neither libgit2 nor shelling out to git is used.","title":"What it is"}]
}
